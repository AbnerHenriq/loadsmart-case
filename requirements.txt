# Local dev: dbt, ingestion script, notebooks
# Airflow and Superset run only inside Docker (see docker-compose.yml)
dbt-core==1.9.0
dbt-duckdb==1.9.6
duckdb==1.1.0
pandas==2.2.2
python-dateutil==2.9.0

# Jupyter
jupyter==1.1.1
ipykernel==6.29.5

# Notebook helper (split_lane demo)
paramiko==3.4.0
